plugins {
    id "com.moowork.node" version "1.2.0"
}

node {
    // Version of node to use.
    version = '7.6.0'

    // Enabled the automatic download.
    download = true
}

build.dependsOn([npm_install, npm_run_build])

